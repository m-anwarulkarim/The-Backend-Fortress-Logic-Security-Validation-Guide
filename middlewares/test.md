---

### ১. মূল প্যাকেজসমূহ (Dependencies)

এই প্যাকেজগুলো আপনার অ্যাপ্লিকেশনের রানটাইমে ব্যবহার হবে:

```bash
npm install express jsonwebtoken @prisma/client redis
```

---

### ২. টাইপস এবং ডেভেলপমেন্ট টুলস (Dev Dependencies)

TypeScript-এ কাজ করার জন্য এই প্যাকেজগুলোর টাইপ ডেফিনিশন এবং Prisma CLI প্রয়োজন:

```bash
npm install -D typescript ts-node-dev @types/express @types/jsonwebtoken @types/node prisma
```

---

### ⚙️ কনফিগারেশন স্টেপস

প্যাকেজ ইনস্টল করার পর নিচের ধাপগুলো অনুসরণ করুন:

#### ১. এনভায়রনমেন্ট ভ্যারিয়েবল (.env)

প্রজেক্টের রুট ডিরেক্টরিতে একটি `.env` ফাইল তৈরি করুন এবং নিচের ভ্যালুগুলো সেট করুন:

```env
DATABASE_URL="your_database_url"
JWT_SECRET="your_super_secret_key"
REDIS_URL="redis://localhost:6379"
```

#### ২. Prisma ইনিশিয়ালাইজেশন

ডাটাবেজ স্কিমা তৈরি করতে নিম্নলিখিত কমান্ড দিন:

```bash
npx prisma init
```

#### ৩. Redis সেটআপ (Local)

যদি আপনার পিসিতে Redis ইনস্টল না থাকে, তবে Redis সার্ভার রান করতে হবে। চাইলে **Docker** দিয়ে সহজে রান করানো যায়:

```bash
docker run -d --name redis-server -p 6379:6379 redis
```

---

### 📂 ফাইল স্ট্রাকচার চেক

নিশ্চিত হয়ে নিন যে ফাইলগুলো এই ফোল্ডারগুলোতে আছে:

- `src/middleware/authGuard.ts`
- `src/utils/redis.ts`
- `src/utils/jwt.ts`
- `src/lib/prisma.ts`
- `src/types/express.d.ts`

---

### 💡 প্রো-টিপ

সবকিছু ইনস্টল করার পর আপনার `package.json` ফাইলের `scripts` সেকশনে নিচের কমান্ডটি যোগ করুন যাতে প্রজেক্ট সহজে রান করা যায়:

```json
"scripts": {
  "dev": "ts-node-dev --respawn --transpile-only src/server.ts"
}
```

---
